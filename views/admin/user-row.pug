tr(id=""+ id)
    td.text-primary
        span.show-form
            i.fas.fa-edit.fa-2x
    td= username
    td= roles[role]
    td= state
    td= moment(createdAt).format("LLL")
    td.text-danger.text-center
        i.fas.fa-trash-alt.fa-2x